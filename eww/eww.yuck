;; Polls
(defpoll music-info :interval "3s" "./scripts/music.sh")
(defpoll music-status :interval "1s" "mpc status | awk 'NR==2 {print $1}' | tr -d '[]'")

;; Vars
(defvar music-control false)
;; (defvar distro-reveal false)

;; Widgets
(include "modules/profile.yuck")
(include "modules/distro.yuck")
(include "modules/try.yuck")
